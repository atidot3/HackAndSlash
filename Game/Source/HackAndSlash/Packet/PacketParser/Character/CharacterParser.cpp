#include "HackAndSlash.h"
#include "HackAndSlashPlayerController.h"
#include "AI/Navigation/NavigationSystem.h"

void AHackAndSlashPlayerController::RecvCharacterLoad(/*NtlPAcket *pk*/)
{
	/*if (pk == nullptr)
		return;
	CU_CHARACTER_LOAD_RESPONCE *res = new CU_CHARACTER_LOAD_RESPONCE;
	memcpy(res, pk->pk, sizeof(CU_CHARACTER_LOAD_RESPONCE));

	if (res->byCount <= 0)
		CharacterLoadResult(false);*/
	// FILL STRUCT
}
